# dependencies
node_modules

# next.js / build output
.next
dist
out
coverage

# env files — never commit secrets; .env.example is safe to share
.env
.env.local
.env.*.local

# vercel
.vercel

# logs & OS
*.log
.DS_Store

# typescript / tooling noise
*.tsbuildinfo

# large local archives (do not include in shared project ZIPs)
*.zip
