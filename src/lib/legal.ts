/**
 * Legal / policy dates — update only when the policy text changes.
 * (Do not use `new Date()` on the page; that would always show “today”.)
 */
export const LEGAL_LAST_UPDATED = "July 17, 2026";
export const LEGAL_LAST_UPDATED_ISO = "2026-07-17";
